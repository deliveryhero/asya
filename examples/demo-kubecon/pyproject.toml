[project]
name = "asya-demo"
version = "0.1.0"
description = "KubeCon demo: evaluator-optimizer flow on Asya actor mesh"
requires-python = ">=3.13"
dependencies = [
  "litellm[google]>=1.82.2",
  "tenacity>=9.0.0",
]

[tool.setuptools.packages.find]
where = ["src"]
