templates:
  namespace: default
  router_image: "python:3.13-slim"
  max_replicas: 5

compiler:
  routers: "./compiled/${arg:flow_name}"
  manifests: "./compiled/${arg:flow_name}/manifests"
  templates: ".asya/templates"

build:
- module: "*"
  image: "examples.asya.sh/*:latest"
