[project]
name = "dspy-examples"
version = "0.1.0"
description = "Minimal examples using DSPy with ReAct and ChainOfThought patterns"
requires-python = ">=3.10"
dependencies = [
    "dspy-ai>=3.1.2",
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
