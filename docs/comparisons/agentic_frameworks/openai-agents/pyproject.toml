[project]
name = "openai-agents-examples"
version = "0.1.0"
description = "Minimal examples using OpenAI Agents SDK"
requires-python = ">=3.9"
dependencies = [
    "openai-agents>=0.7.0",
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
