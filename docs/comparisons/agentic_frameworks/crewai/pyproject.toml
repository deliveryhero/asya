[project]
name = "crewai-examples"
version = "0.1.0"
description = "CrewAI agent examples and comparisons"
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai>=0.30.0",
    "python-dotenv>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
