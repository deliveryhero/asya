services:
  stub-ai:
    build: .
    ports:
      - "8100:8100"
    volumes:
      - ./config:/opt/vidaimock/config:ro
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8100/health"]
      interval: 5s
      timeout: 3s
      retries: 3
