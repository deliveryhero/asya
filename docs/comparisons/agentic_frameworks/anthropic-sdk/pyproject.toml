[project]
name = "anthropic-sdk-examples"
version = "0.1.0"
description = "Minimal examples using Anthropic SDK with tool use pattern"
requires-python = ">=3.9"
dependencies = [
    "anthropic>=0.28.0",
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
