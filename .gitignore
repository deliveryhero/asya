# Managed by git-aint — overwritten on every `git aint init`
# On aint-sync branch: only track epics/
# Everything else (source code, docs, etc.) lives on main branch
*
!.gitignore
!epics/
!epics/**

# Tmux session logs (local only) — must come AFTER !epics/** to override it
epics/**/.tmux-logs/
