{{- if .Values.mcp.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "asya-gateway.mcp.fullname" . }}
  labels:
    {{- include "asya-gateway.labels" . | nindent 4 }}
    app.kubernetes.io/component: mcp
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.mcp.port }}
    targetPort: mcp
    protocol: TCP
    name: http
    {{- if and .Values.service.mcp.nodePort (eq .Values.service.type "NodePort") }}
    nodePort: {{ .Values.service.mcp.nodePort }}
    {{- end }}
  selector:
    {{- include "asya-gateway.selectorLabels" . | nindent 4 }}
{{- end }}
