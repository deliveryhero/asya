apiVersion: v2
name: asya-gateway
description: Asya MCP Gateway with PostgreSQL backend
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: postgresql
    version: "~15.5.0"
    repository: https://charts.bitnami.com/bitnami
    condition: postgresql.enabled
