{{- if and .Values.sampleStorages.minio.enabled (eq .Values.global.storage "minio") .Values.sampleStorages.minio.persistence.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-data
  namespace: {{ .Release.Namespace }}
  labels:
    app: minio
    app.kubernetes.io/name: minio
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.sampleStorages.minio.persistence.size }}
{{- end }}
