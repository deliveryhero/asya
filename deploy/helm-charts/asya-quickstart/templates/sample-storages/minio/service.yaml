{{- if and .Values.sampleStorages.minio.enabled (eq .Values.global.storage "minio") }}
apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: {{ .Release.Namespace }}
  labels:
    app: minio
    app.kubernetes.io/name: minio
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: {{ .Values.sampleStorages.minio.service.type }}
  ports:
  - port: {{ .Values.sampleStorages.minio.service.apiPort }}
    targetPort: api
    protocol: TCP
    name: api
  - port: {{ .Values.sampleStorages.minio.service.consolePort }}
    targetPort: console
    protocol: TCP
    name: console
  selector:
    app: minio
{{- end }}
