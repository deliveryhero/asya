{{- if and .Values.sampleStorages.s3Localstack.enabled (eq .Values.global.storage "s3") }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: localstack-s3
  namespace: {{ .Release.Namespace }}
  labels:
    app: localstack-s3
    app.kubernetes.io/name: localstack-s3
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: sample-storage
spec:
  replicas: 1
  selector:
    matchLabels:
      app: localstack-s3
  template:
    metadata:
      labels:
        app: localstack-s3
    spec:
      containers:
      - name: localstack
        image: {{ .Values.sampleStorages.s3Localstack.image.repository }}:{{ .Values.sampleStorages.s3Localstack.image.tag }}
        ports:
        - containerPort: 4566
          name: edge
        env:
        - name: SERVICES
          value: "s3"
        - name: DEBUG
          value: "0"
        - name: DATA_DIR
          value: /tmp/localstack/data
        resources:
          {{- toYaml .Values.sampleStorages.s3Localstack.resources | nindent 10 }}
        readinessProbe:
          httpGet:
            path: /_localstack/health
            port: 4566
          initialDelaySeconds: 10
          periodSeconds: 5
{{- end }}
