{{- $checkpointS3 := index .Values "checkpoint-s3" }}
{{- if $checkpointS3.enabled }}
apiVersion: asya.sh/v1alpha1
kind: AsyncActor
metadata:
  name: {{ $checkpointS3.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "asya-crew.checkpoint-s3.labels" . | nindent 4 }}
  {{- with $checkpointS3.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  actor: {{ $checkpointS3.name }}
  {{- include "asya-crew.pubsub-spec" . | nindent 2 }}
  {{- if .Values.persistence.enabled }}
  flavors:
    - {{ include "asya-crew.persistence.flavorName" . }}
  {{- end }}

  {{- if or .Values.tracing.endpoint .Values.tracing.protocol }}
  tracing:
    {{- if .Values.tracing.endpoint }}
    endpoint: {{ .Values.tracing.endpoint | quote }}
    {{- end }}
    {{- if .Values.tracing.protocol }}
    protocol: {{ .Values.tracing.protocol | quote }}
    {{- end }}
  {{- end }}

  {{- with $checkpointS3.sidecar }}
  sidecar:
    {{- toYaml . | nindent 4 }}
  {{- end }}

  {{- if $checkpointS3.scaling.enabled }}
  scaling:
    {{- toYaml $checkpointS3.scaling | nindent 4 }}
  {{- end }}

  image: {{ include "asya-crew.checkpoint-s3.image" . }}
  imagePullPolicy: {{ include "asya-crew.checkpoint-s3.imagePullPolicy" . }}
  handler: asya_crew.checkpointer.handler
  {{- $envList := list }}
  {{- range $key, $value := $checkpointS3.env }}
  {{- if ne $value "" }}
  {{- $envList = append $envList (dict "name" $key "value" $value) }}
  {{- end }}
  {{- end }}
  {{- if $envList }}
  env:
    {{- toYaml $envList | nindent 4 }}
  {{- end }}
  {{- with $checkpointS3.resources }}
  resources:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
