kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: asya-kind
nodes:
  - role: control-plane
    kubeadmConfigPatches:
      - |
        kind: InitConfiguration
        nodeRegistration:
          kubeletExtraArgs:
            node-labels: "ingress-ready=true"
    # extraPortMappings:
    #   # Expose HTTP for potential ingress
    #   - containerPort: 80
    #     hostPort: 8080
    #     protocol: TCP
    #   # Expose HTTPS for potential ingress
    #   - containerPort: 443
    #     hostPort: 8443
    #     protocol: TCP
  - role: worker
  - role: worker
